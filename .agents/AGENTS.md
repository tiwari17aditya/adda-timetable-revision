# Workspace Customizations & Agent Rules for Banking Exam 2026 Mock Tracker

## Core Directives & Token Optimization Guidelines
- **Token Efficiency**: Provide clear, concise, high-density outputs. Avoid wordy introductions, redundant summaries, or boilerplate code duplication.
- **Precision Matching**: Strictly adhere to the 20-category mock catalog (399 total test papers) and IBPS PO Aug 2 - Aug 22 execution schedule.
- **State Integrity**: Maintain `appState.mocks` schema and `appState.ibpsChecked` structure across all modifications.
- **Visual Design Standard**: Use dark-mode glassmorphism styling, clean HSL/CSS tokenized palettes, smooth transitions, and modern typography (`Outfit`, `Plus Jakarta Sans`).
- **Code Standards**: Keep HTML clean and semantic. Keep CSS modular with CSS variables. Wrap JavaScript DOM logic in `safeExecute()` error handlers and maintain event logging to `logEvent()`.
- **Git & CI/CD**: Ensure GitHub Pages automation via `.github/workflows/deploy.yml` remains intact and passes cleanly.
